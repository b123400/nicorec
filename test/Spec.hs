main :: IO ()
main = putStrLn "Test suite not yet implemented"
--  describe "URI parser" $
--   it "should read correctly the port number" $
--      port standard_URL ":80" `shouldBe` Right(Port(80))
--   it "should fail when the port is not a number" $
--      port standard_URL ":yy" `shouldBe` Right(Port(80))
-- map
-- myList.stream().map(MyFunction::add).collect(toList())
-- http :// "domain" /:/ 80
